import React, {PureComponent, ReactNode} from 'react';
import {Text} from 'react-native';

export class ActivityScreen extends PureComponent {
  public render(): ReactNode {
    return <Text>Acá irá el listado de actividades</Text>;
  }
}
